# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# legacy secrets (kept from the old project)
secrets.json
google.json
google1.json
"google (3).json"

# Regenerated diagnostic dump from scripts/backfillIngredients.ts
ingredient-backlog.txt

# Rewrite handoff — plan, spec, step briefs and visual reference. Local guidance,
# not application code.
CLAUDE.md
README c.md
REWRITE-PLAN.md
SPEC.md
steps/
reference/

# self-signed certs from `next dev --experimental-https`
/certificates

# copied from the zxing-wasm package by postinstall, never edited by hand —
# committing it would pin a megabyte that goes stale on the next version bump
/public/zxing_reader.wasm

# the Open Beauty Facts dump: an input to scripts/importObf.ts, not part of the
# app. Re-download it when you next seed — theirs is updated continuously, and
# a copy frozen in here only gets staler
*.openbeautyfacts.org.products.csv.gz
